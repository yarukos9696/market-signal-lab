[project]
name = "market-signal-lab"
version = "0.2.0"
description = "Sanitized public case study for a private crypto-perpetual market-signal research and paper/live system."
requires-python = ">=3.10"
readme = "README.md"
license = { text = "MIT" }

[tool.black]
line-length = 100
